/*
package com.example.demo;

import com.example.demo.Query.QueryEvent;
//import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.repository.CrudRepository;
import org.springframework.stereotype.Repository;


@Repository
public interface QueryStore extends CrudRepository<QueryEvent,Long> {
    Iterable<QueryEvent>  findAllByDrzavaEquals(String drz);
    QueryEvent findByIdEquals(long id);
}*/
